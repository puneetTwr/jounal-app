/**
 * Names of the well-known subdirectories inside the content root.
 * Defined once here so the literal strings are never duplicated
 * across the functions that derive paths from them.
 */
export const JOURNALS_DIRECTORY_NAME = "journals";
export const ATTACHMENTS_DIRECTORY_NAME = "attachments";
export const TEMPLATES_DIRECTORY_NAME = "templates";
